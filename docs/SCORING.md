# Scoring

The `CategoryScore` and `ScoreBreakdown` contracts exist, but no scoring algorithm or weights are
implemented in milestones 1–3.

Future scoring must be deterministic, versioned, configuration-driven, coverage-aware, and fully
explainable. An incomplete scan must not silently receive a favorable score.
