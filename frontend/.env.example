# Public build-time value. Leave empty for same-origin production or Vite's local /api proxy.
# Never place credentials or other secrets in VITE_* variables; they are embedded in static files.
VITE_WEBGUARD_API_URL=

# Optional HTTPS or mailto destination for the professional-review CTA.
VITE_CYBERNET_CONTACT_URL=
